import 'package:flutter/material.dart';

/// Staff shell — Phase 1 stub. Full implementation in Phase 3.
/// This file exists so the router can import it without errors.
/// The actual job list, checklist, photos, and status flow are Phase 3 work.
class StaffShellScreen extends StatelessWidget {
  const StaffShellScreen({super.key, required this.child});
  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
